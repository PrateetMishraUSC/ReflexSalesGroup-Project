# Copy to .env.local and adjust.
# App database (local Postgres.app in development, hosted Postgres in production)
DATABASE_URL=postgres://prateet@localhost:5432/reflex_dev

# Used only by `npm test` so tests never touch real data
TEST_DATABASE_URL=postgres://prateet@localhost:5432/reflex_test
